# FOUNDER ACTIONS — for Joe (driving-friendly)

Short, numbered, one-idea-per-line. Skim it at a red light. The full context lives in
[`MASTER_PLAN.md`](./MASTER_PLAN.md); this is just the do-list.

---

## Do these first (minutes each)

1. **Make the repo public.** Run `bash scripts/gh_launch.sh --public`.
2. **Protect main.** Run `bash scripts/gh_launch.sh --protect-main` (requires CI to pass).
3. **Check it worked.** Run `bash scripts/gh_launch.sh --status`.
4. **Show the plan.** Run `npm run plan` (prints the master plan path).

## Get it in front of people (this week)

5. **Prove it's green.** Run `npm run ci` — must pass.
6. **Open the landing page.** Run `npm run marketing`, then open the printed file.
7. **Turn on the waitlist.** Run `npm run waitlist`, then open the printed file.
8. **Deploy the box.** Follow `deploy/HOSTINGER.md` (`sudo bash deploy/bootstrap_hostinger.sh`).
9. **Record the 60-second video.** Script: `marketing/DEMO_VIDEO.md`. Open on 14.3Q, show one
   legal route (billed) + one illegal route (BLOCKED), end on the three CTAs.
10. **Start outreach.** Pick 10 names in regulated/agent-curious teams. Book intro calls.

## Say it right (never break these)

11. **Lead with:** "14.3 quadrillion — when all five legs fire."
12. **Always add the footnote:** "topology × 7B capacity, not a trained weight file."
13. **Never say:** "14.3Q trained weights" or a bare "119M× cheaper" (only with the CapEx formula).
14. **The pitch:** governed, auditable, idle-cheap — ship agents compliance will approve.

## One-liners for the road

15. **The number:** 14.3Q ≈ 2,867× a ~5T frontier class.
16. **The price:** $9.99 Colab + ~$5 HDV ≈ $15/mo.
17. **The moat:** everyone builds a bigger brain; we build a bigger government.
18. **BYOK:** bring your keys, HDV fee $0 — your data never touches our infra.

---

*Full detail: [`MASTER_PLAN.md`](./MASTER_PLAN.md) · Launch runbook:
[`LAUNCH_CHECKLIST.md`](./LAUNCH_CHECKLIST.md) · Copy rules: [`MESSAGING.md`](./MESSAGING.md).*
