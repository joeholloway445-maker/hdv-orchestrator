{
  "name": "hdv-workflow-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Distributed Workflow Automation Platform — n8n-style DAG executor with BullMQ, Prisma, React Flow",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:all": "concurrently \"npm run dev -w packages/api\" \"npm run dev -w packages/worker\" \"npm run dev -w packages/frontend\"",
    "dev:backend": "concurrently \"npm run dev -w packages/api\" \"npm run dev -w packages/worker\"",
    "build": "npm run build --workspaces --if-present",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "migrate": "npm run migrate -w packages/api",
    "test": "node --require ts-node/register --test tests/security.test.ts"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
